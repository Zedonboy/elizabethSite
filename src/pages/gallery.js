import React from "react"
import Layout from "../components/layout"

const Gallery = () => (
  <Layout>
    <section className="w-full text-center p-24">
      <h4>No Photos</h4>
    </section>
  </Layout>
)

export default Gallery
